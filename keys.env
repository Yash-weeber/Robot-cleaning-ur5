GOOGLE_API_KEY_1 = "api1"
GOOGLE_API_KEY_2 = "api1"
GOOGLE_API_KEY_3 = "api1"
GOOGLE_API_KEY_4 = "api1"
GOOGLE_API_KEY_5 = "api1"
GOOGLE_API_KEY_6 = "api1"
GOOGLE_API_KEY_7 = "api1"



To update the **README.md** to reflect the **Headless vs. GUI** simulation options, we need to add a section explaining how the `ViewerAdapter` and the `MUJOCO_GL` environment variables control the visual output.

Here is the updated **README.md** section for managing simulation visibility:

---

## 🖥️ Simulation Display: GUI vs. Headless Mode

The system is configured to handle both **GUI (visual)** and **Headless (background)** simulations. This is useful for running large batches of iterations on a server or saving GPU resources.

### **1. Running Headless (No Window)**

If you want to run the optimization as fast as possible without a 3D window appearing:

* **Automatic Detection:** The `ViewerAdapter` will automatically switch to **HEADLESS mode (EGL)** if it detects no monitor or if the display initialization fails.
* **Manual Force:** You can force headless mode by uncommenting these lines in your main entry scripts (e.g., `main_llm.py`):
```python
import os
os.environ["MUJOCO_GL"] = "egl"
os.environ["PYOPENGL_PLATFORM"] = "egl"

```



### **2. Running with GUI (Visual Window)**

To watch the robot perform the cleaning task in real-time:

* **Requirements:** Ensure you are running on a machine with a monitor and that `has_display` is set to `True` in `env/adapter.py`.
* **Viewer Options:** The system will first try to use the official `mujoco.viewer` (DeepMind) and will fall back to `mujoco-python-viewer` (Community) if needed.

---

## ⚙️ Configuration: Choosing Your Model

Your `config.yaml` manages the "Brain" of the operation. You can switch between **Gemini** and **Ollama** by editing the `llm_settings`:

* **Gemini (Cloud):** Best for high-performance reasoning using `gemini-2.5-flash`.
* **Ollama (Local):** Best for offline testing using models like `gpt-oss:20b`.

| Setting | Value | Description |
| --- | --- | --- |
| `gemini_model` | `"gemini-2.5-flash"` | The Google model used if API keys are provided. |
| `ollama_model` | `"gpt-oss:20b"` | The local model used via Ollama. |
| `n_warmup` | `20` | Number of initial iterations to bootstrap the learning process. |

---

## 📂 Project Structure & Logic

| Folder / File | Description |
| --- | --- |
| **`agent/`** | The "Brain" - Handles the `LLMInterface` for Gemini/Ollama and DMP logic. |
| **`env/`** | The "Body" - Contains the `ViewerAdapter` for GUI/Headless switching and IK solvers. |
| **`runner/`** | The "Director" - Manages the `LLM_Brain` and the optimization loops. |
| **`config/`** | The "Rules" - YAML files defining robot home positions and table boundaries. |

---

## 📊 Logging & Data Analysis

Every experiment creates a unique, numbered folder within `/logs/`.

* **`weights_history.csv`**: Logs every set of weights proposed by the AI vs. those actually executed.
* **`ee_trajectory.csv`**: The high-resolution X, Y, Z path of the robot's "hand" (end-effector).
* **`llm_iteration_log.csv`**: The primary "Scoreboard" showing balls cleaned or human rewards per iteration.
* **`ik_errors.csv`**: Records failures where the robot couldn't physically reach a target point.
* **`llm_dialog/`**: A "Transcript" folder containing every prompt sent to the AI and its raw response.

---

**Would you like me to add a specific section on how to troubleshoot "EGL" errors if the headless mode doesn't start correctly on your GPU?**